name: Build Bitcoin Paper Wallet

env:
  ANDROID_HOME: /usr/local/lib/android/sdk
  ANDROID_SDK_ROOT: /usr/local/lib/android/sdk
  PATH: $ANDROID_HOME/cmdline-tools/latest/bin:$ANDROID_HOME/platform-tools:$PATH

on:
  workflow_dispatch:

jobs:
  build:
    name: Build APK Thủ Công
    runs-on: ubuntu-latest
    steps:
      # ✅ Cài tar/unzip cho người dùng hiện tại, không cần sudo
      - name: Cài công cụ giải nén
        run: |
          cd ~
          wget -q http://archive.ubuntu.com/ubuntu/pool/main/t/tar/tar_1.34+dfsg-3_amd64.deb -O tar.deb
          dpkg -x tar.deb ~/tar
          echo "$HOME/tar/usr/bin" >> $GITHUB_PATH
          rm tar.deb

      - name: Lấy mã nguồn
        uses: actions/checkout@v4

      - name: Cài JDK 11
        uses: actions/setup-java@v4
        with:
          java-version: '11'
          distribution: 'zulu'
          cache: 'gradle'

      - name: Cài Android Command Line Tools & SDK
        run: |
          chmod +x gradlew
          mkdir -p $ANDROID_HOME/cmdline-tools
          cd $ANDROID_HOME/cmdline-tools
          wget -q https://dl.google.com/android/repository/commandlinetools-linux-11076798_latest.zip -O cmdline.zip
          unzip -q cmdline.zip
          mv cmdline-tools latest 2>/dev/null || true
          rm cmdline.zip
          yes | sdkmanager "platforms;android-37" "build-tools;37.0.0" "platform-tools"
          sdkmanager --licenses || true

      - name: Thực thi build
        run: ./gradlew clean assembleDebug --no-daemon --stacktrace

      - name: Đổi tên APK
        if: success()
        run: |
          APK=$(find app/build/outputs/apk/debug -name "*.apk" -type f | head -1)
          mv "$APK" Bitcoin-Paper-Wallet-offline.apk

      - name: Lưu APK
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: Bitcoin-Paper-Wallet-APK
          path: Bitcoin-Paper-Wallet-offline.apk
          retention-days: 30
